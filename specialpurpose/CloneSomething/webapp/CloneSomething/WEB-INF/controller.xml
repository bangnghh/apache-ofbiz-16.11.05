<?xml version="1.0" encoding="UTF-8"?>
<site-conf xmlns:xsi="http://www.w3.org/2001/XMLSchema-instance"
        xmlns="http://ofbiz.apache.org/Site-Conf" xsi:schemaLocation="http://ofbiz.apache.org/Site-Conf http://ofbiz.apache.org/dtds/site-conf.xsd">
    <!-- The controller elements that are common to all OFBiz components
         can be found in the following xml file. A component can override the
         elements found in the common-controller.xml file. -->
    <include location="component://common/webcommon/WEB-INF/common-controller.xml"/>

    <description>CloneSomething Component Site Configuration File</description>

    <!-- Events to run on every request before security (chains exempt) -->
    <!--
    <preprocessor>
    </preprocessor>
    -->
    <!-- Events to run on every request after all other processing (chains exempt) -->
    <!--
    <postprocessor>
        <event name="test" type="java" path="org.apache.ofbiz.webapp.event.TestEvent" invoke="test"/>
    </postprocessor>
    -->

    <!-- Request Mappings -->
    <request-map uri="main"><security https="true" auth="true"/><response name="success" type="view" value="main"/></request-map>

    <!-- Request Mappings For Add Product Form -->

    <!--<request-map uri="createDemProduct">
        <security https="true" auth="true"/>
        <event type="service" invoke="createDemProduct"/>
        <response name="success" type="view" value="main"></response>
    </request-map>-->

    <request-map uri="createDemProductInJava">
        <security https="true" auth="true"/>
        <event type="service" invoke="createDemProductInJava"/>
        <response name="success" type="view" value="main"></response>
    </request-map>

    <request-map uri="createDemProductEvent">
        <security auth="true" https="true"/>
        <event type="java" path="com.example.ofbiz.events.CreateEvents" invoke="createDemProductEvent"/>
        <response name="success" type="view" value="main"/>
        <response name="error" type="view" value="main"/>
    </request-map>

    <request-map uri="createDemProductTypeEvent">
        <security auth="true" https="true"/>
        <event type="java" path="com.example.ofbiz.events.CreateTypeEvent" invoke="createDemProductTypeEvent"/>
        <response name="success" type="view" value="main"/>
        <response name="error" type="view" value="main"/>
    </request-map>

    <!-- Request Mappings For Add Product Type Form -->
    <request-map uri="createDemProductType">
        <security https="true" auth="true"/>
        <event type="service" invoke="createDemProductType"/>
        <response name="success" type="view" value="main"></response>
    </request-map>

    <!-- Request Mappings For Find Form -->

    <request-map uri="findDemProduct">
        <security https="true" auth="true"/>
        <response name="success" type="view" value="findDemProduct"></response>
    </request-map>

    <!-- View Mappings -->
    <view-map name="main" type="screen" page="component://CloneSomething/widget/CloneSomethingScreens.xml#main"/>

    <view-map name="findDemProduct" type="screen" page="component://CloneSomething/widget/CloneSomethingScreens.xml#findDemProduct"/>
</site-conf>